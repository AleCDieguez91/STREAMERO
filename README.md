
  # Crear prototipo desde documento

  This is a code bundle for Crear prototipo desde documento. The original project is available at https://www.figma.com/design/87Evc79t4MjbWiMFyfYZCM/Crear-prototipo-desde-documento.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  